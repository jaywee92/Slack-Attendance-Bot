services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - session_data:/session
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    environment:
      - TZ=Europe/Berlin
      - ATTENDANCE_DEBUG_SCREENSHOT=/session/attendance_debug.png
      - ATTENDANCE_DEBUG_HTML=/session/attendance_debug.html

  bot-vnc:
    build:
      context: .
      dockerfile: Dockerfile.vnc
    env_file: .env
    volumes:
      - session_data:/session
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    environment:
      - TZ=Europe/Berlin
      - HEADLESS=false
      - VNC_PASSWORD=change-me
    ports:
      - "5900:5900"
      - "6080:6080"
    profiles:
      - debug

volumes:
  session_data:
